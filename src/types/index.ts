export type Job = {id:number;company:string;logo:string;color:string;role:string;location:string;salary:string;match:number;tags:string[]};
export type Application = {id:number;company:string;role:string;stage:string};
